server {
    listen 8080;
    server_name localhost;

    location /api/ {
        default_type application/json;
        return 501 '{"error":"Replace this placeholder with your real API"}';
    }
}
