# Упрощённый образ: без nginx, только Vite preview (если основной Dockerfile не собирается)
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
ENV VITE_API_URL=
RUN npm run build
EXPOSE 4173
CMD ["npm", "run", "preview"]
